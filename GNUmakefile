# ArmoniOS top-level build policy.
#
# GNU make prefers this file over Makefile. Keep the actual build rules in
# Makefile and define only project-wide defaults here.

# 128 KiB gives the kernel, embedded applications, and desktop foundations
# enough development headroom while retaining a hard, measurable size gate.
KERNEL_SIZE_LIMIT ?= 131072

include Makefile
