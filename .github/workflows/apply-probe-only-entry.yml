name: Apply probe-only kernel entry

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: agent/el0-ipc-argv-usercopy

      - name: Apply exact substitution
        run: |
          if [[ -f tools/apply_probe_only_kernel_main.py ]]; then
            python3 tools/apply_probe_only_kernel_main.py
          else
            echo "probe-only entry already applied"
          fi

      - name: Commit generated entry change
        run: |
          if git diff --quiet; then
            echo "no generated changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Stop the physical RPi4 probe after diagnostics"
          git push origin HEAD:agent/el0-ipc-argv-usercopy
