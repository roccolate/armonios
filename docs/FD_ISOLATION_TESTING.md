# Descriptor isolation testing

Run `bash tests/run_vfs_process_fd_test.sh` for the focused production-code
harness, then run `bash tools/verify.sh` for the complete automated baseline.
The visible FAT/editor workflow remains a separate manual gate.
