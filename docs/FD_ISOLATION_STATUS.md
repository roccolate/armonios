# Descriptor isolation status

Status: **IMPLEMENTED ON BRANCH; FULL REPOSITORY VERIFICATION PENDING**.

Branch: `agent/p0-process-local-fds`

The focused production-code harness has passed with warnings treated as errors.
The branch must remain unmerged until the full `bash tools/verify.sh` baseline
and the visible files/editor/FAT workflow pass on the exact head commit.
