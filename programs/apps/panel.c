// ArmoniOS app: panel (C version, on libkarm + libkarmdesk)
//
// The implementation is kept in one included translation-unit fragment so the
// panel remains one KLI1 app object while its menu/taskbar runtime can evolve
// without turning this entry file into another monolith.

#include "panel_runtime.inc"
